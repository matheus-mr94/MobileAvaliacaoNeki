import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input:{
        color: '#000',
        fontSize: 20,
        borderRadius:10,
        borderWidth:1,              
        height:50,
        flexDirection:'row',
        paddingHorizontal:15,
        backgroundColor: '#fff',
        marginTop:20
        
    }
    
})